#include <stdio.h>

//register

/*
//register int reg_val=60; //error: address of register variable 'var' requested
int main(void)
{
    register int var=50;  // CPU Registers 
    // var is to be stored inside CPU register
     // if prgoram have received memory inside the CPU register 
     // processing wil be fast , Programmer can enjoy speedy performance

     // if CPU register is not allocated ,
     // It will waste time 
     
     printf("Var = %d",var);
     var=60; //allowed 

     //printf("address of register variable : %u",&var); //error
     // error: address of register variable 'var' requested
}

*/