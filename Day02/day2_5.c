#include<stdio.h>

/*
int main(void)
{
    //int val=129;
    // 0 to 255
    // -128 to 127
    int val=125; 
    char *cptr;
    cptr=&val; // cptr is holding address of val 
    printf("val = %d *cptr = %d",val,*cptr);
    return 0;
}
*/
/*
int main(void)
{
    int val=10;
    char ch=12;
    char *cptr;
    cptr=&val; // cptr is holding address of val 
    printf("val = %d *cptr = %d",val,*cptr);
    cptr=&ch; // cptr is holding address of ch 
    printf("\n ch = %d *cptr = %d",ch,*cptr);
    return 0;
}

*/