#include<stdio.h>

/*
int main(void)
{
    //int a=20; // local variable to main() 
    auto int a=20; // recevies memory from stack area 
    // Local variables are called as auto variables 

    printf("Value of a : %d",a); 
    return 0;
}
 //printf("Value of a : %d",a); // not allowed 
// variable "a" can be accessed only within main()
// Scope of variable "a" is only main() (BLOCK) 
// Life of variable "a " is also main() from line number 5 to line number 10
// SCOPE AND LIFE of auto variable is BLOCK 
*/